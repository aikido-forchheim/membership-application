<Page
    x:Class="MembershipApplication.Views.SamplePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MembershipApplication.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
      <TextBlock Text="{Binding Message}" Margin="20" FontSize="30" />
      <CheckBox IsChecked="{Binding RememberMe2, Mode=TwoWay}" />
      <CheckBox IsChecked="{Binding RememberMe, Mode=TwoWay}" />
      <Button Command="{Binding FirstCommand}" Content="Test" />
    </StackPanel>
</Page>
